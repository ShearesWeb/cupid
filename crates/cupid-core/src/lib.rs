//! Library surface for the cupid CCA matcher. The binary (`src/main.rs`) and the
//! integration tests (`tests/`) both consume the crate through these modules.
pub mod algorithm;
pub mod data;
pub mod export;
pub mod models;
pub mod snapshot;
